#services pour communiquer entre le redis master et les autres redis replicas? 
apiVersion: v1
kind: Service
metadata:
  name: redis-master
spec:
  selector:
    app: redis-master
  ports: 
    - port: 6379
      targetPort: 6379
      protocol: TCP
  type: ClusterIP